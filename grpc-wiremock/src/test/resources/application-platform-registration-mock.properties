# Profile for PlatformRegistrationClientMockIntegrationTest
# Enable bean creation at BUILD time (required for @IfBuildProperty)
# The PlatformRegistrationClientMockTestResource will set enabled=true at RUNTIME
# But we set it to false here to prevent onStart() from running during app startup
service.registration.enabled=true
